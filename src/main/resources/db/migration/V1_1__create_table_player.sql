CREATE TABLE Player (
    id BIGINT AUTO_INCREMENT NOT NULL,
    name VARCHAR(100) NOT NULL,
    pace int NOT NULL,
	shoot int NOT NULL,
	pass int NOT NULL,
	drible int NOT NULL,
	defense int NOT NULL,
	physical int NOT NULL
);